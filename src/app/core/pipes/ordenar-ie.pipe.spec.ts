import { OrdenarIEPipe } from './ordenar-ie.pipe';

describe('OrdenarIEPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarIEPipe();
    expect(pipe).toBeTruthy();
  });
});
